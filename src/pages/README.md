# Pages Directory

This directory contains **Top-Level Page Components** (Views).

## Usage
- Each file here represents a route in the application (e.g., `Home.tsx`, `Login.tsx`, `Dashboard.tsx`).
- Pages should primarily compose other components and handle route-specific logic.
