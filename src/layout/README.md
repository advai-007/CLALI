# Layout Directory

This directory contains **layout components** that structure the application pages.

## Usage
- Components that define the "shell" of a page (e.g., `MainLayout`, `AuthLayout`).
- Includes Headers, Footers, Sidebars within the layout wrappers.
