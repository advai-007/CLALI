# Utils Directory

This directory contains **utility functions** and helper classes.

## Usage
- Pure functions (logic that doesn't depend on React state).
- Formatters, validators, API wrappers, constants.
