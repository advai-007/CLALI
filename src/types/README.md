# Types Directory

This directory contains shared **TypeScript definitions**.

## Usage
- Interfaces, Types, and Enums used across multiple files.
- `index.ts` can export all types, or use specific files like `user.types.ts`.
