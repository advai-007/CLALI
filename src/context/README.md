# Context Directory

This directory contains **React Context** definitions and providers.

## Usage
- Use this for global state management (e.g., UserAuth, Theme, ToastNotifications).
- Create a separate file for each context (e.g., `AuthContext.tsx`).
- Export the Provider component and a custom hook to consume the context.
